# requirements.txt — Dependencias unificadas para parking-edge.
# Instalar con: pip install -r requirements.txt

# ── MQTT ─────────────────────────────────────────────────────────────────────
paho-mqtt>=1.6.1

# ── Visión artificial ────────────────────────────────────────────────────────
# BeaglePlay ARM: usar opencv-python-headless para evitar dependencias de GUI
opencv-python-headless>=4.8.0
numpy>=1.24.0
tensorflow>=2.14.0

# YOLO INT8 TFLite — inferencia con TensorFlow Lite nativo
# El modelo se carga directamente desde un archivo .tflite

# OCR
pytesseract>=0.3.10
Pillow>=9.0.0

# ── Dashboard web ─────────────────────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0
flask-login>=0.6.0

# ── Configuración y utilidades ─────────────────────────────────────────────────
PyYAML>=6.0
python-dateutil>=2.8.2
psutil>=5.9.0

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-mock>=3.10.0

# typing_extensions para Python < 3.11
typing-extensions>=4.0.0
